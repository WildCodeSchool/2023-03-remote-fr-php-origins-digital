<div class="container-fluid p-4">
    <div class="row">
        <h2 class="p-4">Tendance</h2>
        {% for video in sortedVideos|slice(0, 6) %}
            {% include '_includes/_cardVideo.html.twig' %}
        {% endfor %}
    </div>
    <div class="d-flex justify-content-center mt-4">
        <a href="{{ path('app_tendances') }}" class="btn btn-info btn-lg my-3 text-secondary">
            Voir plus de vidéos
        </a>
    </div>
</div>
