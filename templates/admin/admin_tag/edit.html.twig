{% extends 'admin/base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <div class="mb-4">
        <a class="text-decoration-none text-white" href="{{ path('app_admin_tag_index') }}">
            <i class="fa-solid fa-arrow-turn-up fa-rotate-270 text-secondary"></i> Liste des tags</a>
    </div>
    <h1>Modifier Tag {{ tag.name }}</h1>

    {{ include('admin/admin_tag/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('admin/admin_tag/_delete_form.html.twig') }}
{% endblock %}
