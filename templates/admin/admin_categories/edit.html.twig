{% extends 'admin/base.html.twig' %}

{% block title %}Modifier la categories {{ category.name }}{% endblock %}

{% block body %}
    <h1>Modifier la categories</h1>

    {{ include('admin/admin_categories/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    <button class="btn btn-dark mt-3">
        <a href="{{ path('admin_categories_index') }}" class="text-decoration-none text-white">Retour à la liste des
            categories</a>
    </button>

    {{ include('admin/admin_categories/_delete_form.html.twig') }}
{% endblock %}
