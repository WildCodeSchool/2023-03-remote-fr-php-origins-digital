{% extends 'admin/base.html.twig' %}

{% block title %}Modifier la vidéo {% endblock %}

{% block body %}
    <div class="mb-4">
        <a href="{{ path('app_admin_video_index') }}" class="text-decoration-none text-white">
            <i class="fa-solid fa-arrow-turn-up fa-rotate-270 text-secondary"></i> Liste des vidéos</a>
    </div>
    <h1>Modifier la video  {{ video.title }}</h1>

        {{ include('admin/admin_video/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        {{ include('admin/admin_video/_delete_form.html.twig') }}
{% endblock %}

