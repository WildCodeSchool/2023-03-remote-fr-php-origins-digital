{{ form_start(form1) }}
<div class="row text-secondary">
    <div class="col-md-6 mt-4 mb-2">
        {{ form_widget(form1) }}
    </div>
</div>
    <button class="mt-2 mb-2 rounded-2">{{ button_label|default('Créer') }}</button>
{{ form_end(form1) }}
