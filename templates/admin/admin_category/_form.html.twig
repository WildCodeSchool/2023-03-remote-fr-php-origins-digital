{{ form_start(form) }}
{{ form_start(form1) }}

<div class="row text-secondary">
    <div class="col-md-6 mt-4 mb-2">
        {{ form_widget(form1.fileBackground) }}
        {{ form_widget(form1.fileCharacter) }}
        {{ form_widget(form1.fileName) }}
        {{ form_label(form.name, 'Nom de la catégorie') }}
        {{ form_widget(form.name) }}
    </div>
</div>

<button class="mt-2 mb-2 rounded-2" onclick="linkFormValues()">{{ button_label|default('Créer') }}</button>

{{ form_end(form1) }}
{{ form_end(form) }}
<script>
    function linkFormValues() {
        var categoryName = $('#category_name').val();
        $('#image_category_name').val(categoryName);
    }
</script>
