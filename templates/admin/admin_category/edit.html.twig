{% extends 'admin/base.html.twig' %}

{% block title %}Modifier la catégorie {{ category.name }}{% endblock %}

{% block body %}
    <div class="mb-4">
        <a class="text-decoration-none text-white" href="{{ path('app_admin_category_index') }}">
            <i class="fa-solid fa-arrow-turn-up fa-rotate-270 text-secondary"></i> Liste des catégories</a>
    </div>
    <h1>Modifier la catégorie {{ category.name }}</h1>

    {{ include('admin/admin_category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('admin/admin_category/_delete_form.html.twig') }}
    {{ include('admin/admin_image_category/_image.html.twig') }}
{% endblock %}
